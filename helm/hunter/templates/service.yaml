apiVersion: v1
kind: Service
metadata:
  name: {{ include "hunter-portfolio.fullname" . }}
  labels:
    {{- include "hunter-portfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hunter-portfolio.selectorLabels" . | nindent 4 }}
